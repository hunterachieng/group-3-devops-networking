// AWS provider configuration for the lab workload.
// All workload resources must be created in us-west-1.
// Add default tags here so every resource carries the required project,
// group, owner, and environment metadata.

